var fs = require('fs')

module.exports = {
  getAppUptime: function() {
    return process.uptime()
  },
  getSystemUptime: function() {
    return Number(fs.readFileSync('/proc/uptime').toString().split(' ')[0])
  }
}